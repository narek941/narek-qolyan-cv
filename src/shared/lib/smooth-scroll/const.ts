/** Lenis options for free smooth scrolling. */
export const SMOOTH_SCROLL_OPTIONS = {
  smoothWheel: true,
  touchMultiplier: 1.4,
} as const;

/** Duration (seconds) of programmatic scrolls triggered from the nav. */
export const NAV_SCROLL_DURATION_SECONDS = 1.4;
